{
    "type": "object",
    "properties": {
        "email": {
            "type": "string",
            "pattern": "^[a-z0-9.]+@+[gmail]+.com$"
        },
        "username": {
            "type": "string",
            "pattern": "^[a-z0-9.]{0,20}$"
        },
        "password": {
            "type": "string",
            "pattern": "^[a-z0-9.]{0,20}$"
        }
    },
    "additionalProperties": false,
    "required": ["email", "username", "password"]
}